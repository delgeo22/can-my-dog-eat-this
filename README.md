## Can my dog eat this?

Single-page web app with a built-in food safety database for dogs (30 common foods).

### Run it

- Open `index.html` in your browser, or
- From this folder, run:

```bash
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.

